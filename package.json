{
  "name": "zero-latency-blog",
  "version": "1.0.0",
  "scripts": {
    "build": "hugo --minify && node scripts/postbuild.js"
  },
  "dependencies": {
    "glob": "^10.0.0",
    "jsdom": "^22.0.0",
    "katex": "^0.16.0",
    "mermaid-cli": "^8.7.0",
    "@mermaid-js/mermaid-cli": "^10.0.0"
  },
  "devDependencies": {}
}
